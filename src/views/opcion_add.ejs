<%- include('./partials/_header'); %>

<div class="container mt-5">
    <div class="row">
        <div class="col-md-5">
            <div class="card">
                <div class="card-body">
                    <% console.log('El primero data ->') %> 
                    <% console.log(data) %> 
                    <% console.log('El segundo data.id') %> 
                    <% console.log(data) %> 
                    <form action="/opcion/add/<%= data %>" method="post">
                        <input type="hidden" value="<%= data %>" name="id_pregunta">
                        <div class="form-group mt-1">
                            <input type="text" name="texto" placeholder="Texto" class="form-control">
                        </div>
                        <button type="submit" class="btn btn-primary mt-1">
                            Crear Opcion
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>